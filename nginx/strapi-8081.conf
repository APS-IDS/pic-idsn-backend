server {
    listen 8081;

    # Proxy all requests to Strapi service on internal port 1337
    location / {
        proxy_pass http://strapi:1337;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
